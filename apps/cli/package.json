{
  "name": "llm-cli",
  "private": true,
  "type": "module",
  "main": "src/cli.ts",
  "dependencies": {
    "commander": "^11.0.0",
    "@workflow-tool/shared": "workspace:*",
    "@workflow-tool/workflow": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile ../../dist/cli",
    "test": "bun test"
  },
  "bin": {
    "workflow-tool": "./src/cli.ts"
  }
}
